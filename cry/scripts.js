function submitUserInfo(form){
  var userId=form.inputbox1.value;
  var passwd=form.inputbox2.value;
  alert("user info submited");
}

function submitWebInfo(form){
  var website=form.website.value;
  var keywords=form.keywords.value;
  alert("info for website submited");
}